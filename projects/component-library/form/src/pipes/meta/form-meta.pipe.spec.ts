import { FormMetaPipe } from './form-meta.pipe';

describe('FormMetaPipe', () => {
    it('create an instance', () => {
        const pipe = new FormMetaPipe();
        expect(pipe).toBeTruthy();
    });
});
