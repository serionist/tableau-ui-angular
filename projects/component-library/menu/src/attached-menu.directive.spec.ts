import { AttachedMenuDirective } from './attached-menu.directive';

describe('AttachedMenuDirective', () => {
    it('should create an instance', () => {
        const directive = new AttachedMenuDirective();
        expect(directive).toBeTruthy();
    });
});
