import { IsSelectedValuePipe } from './is-selected-value.pipe';

describe('IsSelectedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
