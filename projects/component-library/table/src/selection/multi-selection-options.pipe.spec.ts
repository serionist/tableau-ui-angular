import { MultiSelectionOptionsPipe } from './multi-selection-options.pipe';

describe('MultiSelectionOptionsPipe', () => {
    it('create an instance', () => {
        const pipe = new MultiSelectionOptionsPipe();
        expect(pipe).toBeTruthy();
    });
});
