import { SingleSelectionOptionsPipe } from './single-selection-options.pipe';

describe('SingleSelectionOptionsPipe', () => {
    it('create an instance', () => {
        const pipe = new SingleSelectionOptionsPipe();
        expect(pipe).toBeTruthy();
    });
});
