import { HeaderCheckboxModePipe } from './header-checkbox-mode.pipe';

describe('HeaderCheckboxModePipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderCheckboxModePipe();
    expect(pipe).toBeTruthy();
  });
});
