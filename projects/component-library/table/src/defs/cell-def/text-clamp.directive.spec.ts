import { TextClampDirective } from './text-clamp.directive';

describe('TextClampDirective', () => {
    it('should create an instance', () => {
        const directive = new TextClampDirective();
        expect(directive).toBeTruthy();
    });
});
