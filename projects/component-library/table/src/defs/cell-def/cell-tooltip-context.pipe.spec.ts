import { CellTooltipContextPipe } from './cell-tooltip-context.pipe';

describe('CellTooltipContextPipe', () => {
    it('create an instance', () => {
        const pipe = new CellTooltipContextPipe();
        expect(pipe).toBeTruthy();
    });
});
