import { HeaderToolipDefDirective } from './header-toolip-def.directive';

describe('HeaderToolipDefDirectiveTsDirective', () => {
    it('should create an instance', () => {
        const directive = new HeaderToolipDefDirective();
        expect(directive).toBeTruthy();
    });
});
