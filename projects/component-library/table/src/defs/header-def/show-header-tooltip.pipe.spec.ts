import { ShowHeaderTooltipPipe } from './show-header-tooltip.pipe';

describe('ShowHeaderTooltipPipe', () => {
    it('create an instance', () => {
        const pipe = new ShowHeaderTooltipPipe();
        expect(pipe).toBeTruthy();
    });
});
