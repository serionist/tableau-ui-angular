import { HeaderTooltipContextPipe } from './header-tooltip-context.pipe';

describe('HeaderTooltipContextPipe', () => {
    it('create an instance', () => {
        const pipe = new HeaderTooltipContextPipe();
        expect(pipe).toBeTruthy();
    });
});
