import { HeaderTooltipPipe } from './header-tooltip.pipe';

describe('HeaderTooltipPipe', () => {
    it('create an instance', () => {
        const pipe = new HeaderTooltipPipe();
        expect(pipe).toBeTruthy();
    });
});
