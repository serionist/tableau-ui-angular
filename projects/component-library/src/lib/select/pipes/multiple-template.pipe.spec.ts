import { MultipleTemplatePipe } from './multiple-template.pipe';

describe('MultipleTemplatePipe', () => {
    it('create an instance', () => {
        const pipe = new MultipleTemplatePipe();
        expect(pipe).toBeTruthy();
    });
});
