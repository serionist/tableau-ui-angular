import { FormControlValuePipe } from './form-control-value.pipe';

describe('FormControlValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlValuePipe();
    expect(pipe).toBeTruthy();
  });
});
