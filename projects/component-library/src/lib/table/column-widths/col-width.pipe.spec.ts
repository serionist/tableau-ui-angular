import { ColWidthPipe } from './col-width.pipe';

describe('ColWidthPipe', () => {
    it('create an instance', () => {
        const pipe = new ColWidthPipe();
        expect(pipe).toBeTruthy();
    });
});
