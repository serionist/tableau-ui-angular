import { CellDefDirective } from './cell-def.directive';

describe('CellDefDirective', () => {
  it('should create an instance', () => {
    const directive = new CellDefDirective();
    expect(directive).toBeTruthy();
  });
});
